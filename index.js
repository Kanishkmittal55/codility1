function oddArray(N) {
  // [1,2,1,2,3] =>  A[0]: A[2] , A[1]: A[3]
  // unpaired = A[4]

  // Edge Cases :
  // 1. Empty Array

  //Steps:
  //1. Iterate through the array
  //2. store the values and if a particular value has a match , return the match
  //3. return the value without a match

  let obj = {}; // The reason we use a hashtable because there is no lookup , a particular key or value will map automatically to the a particular value
}
